{
  "name": "chrome-yt-ext",
  "version": "1.1.0",
  "private": true,
  "type": "module",
  "description": "Modular Chrome extension for YouTube. Each feature is a self-contained module.",
  "scripts": {
    "pretty": "prettier --write .",
    "build": "node build.mjs",
    "watch": "node build.mjs --watch",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.268",
    "esbuild": "^0.21.5",
    "prettier": "^3.8.3",
    "typescript": "^5.4.5"
  }
}
